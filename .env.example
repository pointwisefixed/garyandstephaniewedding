# Environment Variables for Production Deployment
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - it's in .gitignore

# Required for production
SECRET_KEY_BASE=your-production-secret-key-here

# Email configuration (optional)
SMTP_ADDRESS=mail.garyandstephanie.com
SMTP_PORT=25
SMTP_USERNAME=wedding@garyandstephanie.com
SMTP_PASSWORD=your-smtp-password-here
SMTP_AUTHENTICATION=plain
MAILER_HOST=garyandstephanie.com

# Database (if using PostgreSQL instead of SQLite)
# DATABASE_URL=postgresql://user:password@localhost/wedding_production

# Rails environment
RAILS_ENV=production
RAILS_LOG_LEVEL=info
